# .github/workflows/deploy.yml
name: Trigger Vercel Deploy Hook

on:
  push:
    branches: [main]

jobs:
  trigger-vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Vercel Deploy Hook
        run: curl -X POST ${{ secrets.NETLIFYL_DEPLOY_HOOK }}
